---
name: chriswa-devkit:session-id
description: Use when the user asks for the current Claude Code session ID or needs to identify the current session.
---

# Current Session ID

The current Claude Code session ID is available in the environment variable:

```
$CLAUDE_SESSION_ID
```

Use `echo $CLAUDE_SESSION_ID` via the Bash tool to retrieve it.
